import { ScoreRatingPipe } from './score-rating.pipe';

describe('ScoreRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new ScoreRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
